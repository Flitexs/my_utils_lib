#include "sort.h"

void bubbleSort(int *arr, int size) {
    if (arr == nullptr || size <= 0) {
        return;
    }
    bool swapped = false;
    for (int i = 0; i < size - 1; ++i) {
        for (int j = 0; j < size - 2; j++) {

        }
    }
}

